// Debug and modify the code so that the console does not display any errors.
// Make the sum at the end add the apples and oranges.

const tao = "unchanging" ;   //could have changed the type to 'let'
//let tao = "i've changed";  //could have deleted this line instead of commenting it out

let apples = 5;
let oranges = 12;

let fruit = apples + oranges;
